abi_version: '0.1'
tool_id: scapp
name: SCAPP
category: plasmid_binning
purpose: Plasmid reconstruction from assembly graph using SCAPP.
when_to_use:
- assembly_graph_available
- plasmid_reconstruction_required
inputs:
  assembly:
    type: file
    format: fasta
    required: true
  assembly_graph:
    type: file
    format: fastg
    required: true
  read1:
    type: file
    format: fastq.gz
    required: true
  read2:
    type: file
    format: fastq.gz
    required: true
  threads:
    type: integer
    minimum: 1
outputs:
  output_dir:
    type: directory
execution:
  executable: scapp
  command_template: scapp -g {assembly_graph} -o {output_dir} -k 77 -p {threads} -r1 {read1} -r2 {read2}
  network: false
  writes_output: true
failure_handling:
  missing_input:
    hint: Check assembly FASTA and GFA graph paths.
  tool_not_found:
    hint: Install SCAPP in the autoplasm-plasmid-binning environment.
  nonzero_exit:
    hint: Inspect SCAPP stderr log in provenance/step_logs.
resources:
  cpu: 4
  memory: 4GB
  walltime: "01:00:00"
